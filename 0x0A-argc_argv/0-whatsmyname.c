#include "main.h"

/**
  * main - This prints the name of a program
  * @argv: argument vector param
  * Return: 0
  */

int main(int __attribute((unused)) argc, char *argv[])
{
	_putchar(argv[0]);
	return (0);
}
